import 'package:flutter/material.dart';

const radiusTop24 = BorderRadius.only(
    topLeft: Radius.circular(24.0), topRight: Radius.circular(24.0));

const radiusTop20 = BorderRadius.only(
    topLeft: Radius.circular(20.0), topRight: Radius.circular(20.0));

const radiusTop15 = BorderRadius.all(
     Radius.circular(1.5));

const radiusTop8 = BorderRadius.all(
    Radius.circular(8.0));

const radiusTop7 = BorderRadius.all(
    Radius.circular(7.0));

const radiusTop4 = BorderRadius.all(
    Radius.circular(4.0));

const radiusTop16 = BorderRadius.only(
    topLeft: Radius.circular(16.0), topRight: Radius.circular(16.0));



/*
const radiusAll16 = BorderRadius.all(Radius.circular(16));

const radiusAll10 = BorderRadius.all(Radius.circular(10));

const radiusAll20 = BorderRadius.all(Radius.circular(20));*/
